import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    hello: {
        fontWeight: 'bold',
        backgroundColor: 'red',
        color: 'black',
    }
});

export default styles;